export default function SettingsPage() {
  return (
    <div className="flex h-full items-center justify-center text-fg-muted">
      Settings placeholder — Claude Design prompt #4 (sibling).
    </div>
  );
}
